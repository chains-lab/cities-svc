
type: object
required:
  - data
properties:
  data:
    type: object
    required:
      - id
      - type
      - attributes
    properties:
      id:
        type: string
        format: uuid
        description: "user id"
      type:
        type: string
        enum: [ update_own_gov ]
      attributes:
        type: object
        properties:
          label:
            type: string
            description: "optional label for the user in this city"