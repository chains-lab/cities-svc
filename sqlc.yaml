version: "2"
sql:
  - schema:
      - "db/migrations"
    queries:
      - "db/queries"
    engine: "postgresql"
    codegen:
      - out: "internal/dbx"
        plugin: "go"
        options:
          package: "dbx"

